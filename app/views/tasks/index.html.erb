<h1> Tasks list </h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
        [X]
       <%else%>
       []
       <% end %>
    <%= link_to task.title, task_path(task) %>
  <%= link_to 'Delete', task_path(task), method: :delete %>
    </li>
  <% end %>
</ul>
 <%= link_to '+ Add new task', task_new_path %>
